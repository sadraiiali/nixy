# Contributor avatars (build-time)

Tiny circular WebP badges for page authors. **Gitignored** — not committed.

```bash
npm run contributors
# or as part of: npm run build
```

Output: `static/icons/contributors/{login}.webp` (~≤1 KiB each).
