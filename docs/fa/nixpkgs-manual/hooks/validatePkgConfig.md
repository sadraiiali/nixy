# validatePkgConfig {#validatepkgconfig}

قلاب `validatePkgConfig` تمامی فایل‌های pkg-config (`.pc`) را در یک بسته اعتبارسنجی می‌کند. این امر به شناسایی برخی از خطاهای رایج در فایل‌های pkg-config، مانند متغیرهای تعریف‌نشده کمک می‌کند.
