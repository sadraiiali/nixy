# scons {#scons}

فازهای ساخت، نصب و بررسی را بازنویسی می‌کند. این بخش از سیستم ساخت scons به عنوان جایگزینی برای make استفاده می‌کند. scons فاز پیکربندی ارائه نمی‌دهد، بنابراین همه چیز در زمان ساخت و نصب مدیریت می‌شود.
