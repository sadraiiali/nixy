# bmake {#bmake-hook}

ابزار [bmake](https://www.crufty.net/help/sjg/bmake.html) گونهٔ قابل‌حمل از ابزار `make` در NetBSD است.

در Nixpkgs، ابزار `bmake` همراه با قلابی ارائه می‌شود که فازهای پیش‌فرض ساخت، بررسی، نصب و توزیع را بازنشانی می‌کند.
