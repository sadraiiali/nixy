# توابع کتابخانه Nixpkgs {#sec-functions-library}

Nixpkgs یک کتابخانه استاندارد در `pkgs.lib` یا از طریق `import <nixpkgs/lib>` ارائه می‌دهد.

<!-- مستندات تولیدشده توسط nixdoc باید هنگام ساخت در اینجا ضمیمه شوند! -->
