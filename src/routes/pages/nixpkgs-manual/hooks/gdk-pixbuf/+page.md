# <a id="setup-hook-gdk-pixbuf"></a> gdk-pixbuf

متغیر محیطی `GDK_PIXBUF_MODULE_FILE` را برای سازنده صادر می‌کند. برای دریافت پشتیبانی از svg، بسته librsvg را به `buildInputs` اضافه کنید. همچنین [توضیحات قلاب راه‌اندازی در اسناد پلتفرم GNOME](#ssec-gnome-hooks-gdk-pixbuf) را ببینید.
