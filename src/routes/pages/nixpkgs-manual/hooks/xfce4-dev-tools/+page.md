# <a id="xfce4-dev-tools"></a> `xfce4-dev-tools`

این قلاب آماده‌سازی تلاش می‌کند `xdt-autogen` را در `xdtAutogenPhase` اجرا کند، که بخشی از `preConfigurePhases` است.

<a id="dontUseXdtAutogenPhase"></a> این رفتار را می‌توان با تنظیم `dontUseXdtAutogenPhase` روی `true` غیرفعال کرد.
